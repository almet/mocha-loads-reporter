{
  "name": "mocha-loads-reporter",
  "repo": "mozilla-services/mocha-loads-reporter",
  "description": "A reporter to send the results to the loads runner",
  "version": "0.0.1",
  "keywords": ["mocha", "test", "reporter"],
  "dependencies": {'zmq', 'JSON'},
  "development": {},
  "license": "MPLv2",
  "scripts": [
    "index.js"
  ]
}
